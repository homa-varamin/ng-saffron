import { TooltipPosition } from '@angular/material/tooltip';
export interface SaffronActionColumnData {
  id?: string;
  icon?: string;
  toolTip?: string;
  toolTipPosition?: TooltipPosition;
  imageUrl?: string;
  imageWidthPx?: string;
  imageHeightPx?: string;
}
